export default {
	namespaced : true,
	state : {
		pathAccount : "admin/accounts"
	},
	actions : {
		async getListAdmin({state}, payload ){
			let response;
			try {
				
			} catch (error) {
				
			}
		}
	}
}